% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/horner.R
\name{polynomial-evaluation}
\alias{polynomial-evaluation}
\alias{horner}
\alias{rhorner}
\alias{naivepoly}
\alias{betterpoly}
\title{Polynomial evaluation algorithms}
\usage{
horner(x, coefs)

rhorner(x, coefs)

naivepoly(x, coefs)

betterpoly(x, coefs)
}
\arguments{
\item{x}{A numeric vector of points at which to evaluate the polynomial.}

\item{coefs}{A non-empty numeric vector of coefficients in ascending power
order: the first element is the constant term, the second is the
coefficient of x, and so on.}
}
\value{
A numeric vector containing the polynomial value at each element of
\code{x}.
}
\description{
Evaluate a polynomial by direct powers, cached powers, iterative Horner
evaluation, or recursive Horner evaluation.
}
\details{
All four functions use the same coefficient convention. For example,
\code{c(5, -3, 2)} represents \code{5 - 3*x + 2*x^2}.

\code{naivepoly()} computes each power independently. \code{betterpoly()} caches the
current power of x. \code{horner()} evaluates iteratively using Horner's rule.
\code{rhorner()} evaluates the same recurrence recursively.

The functions accept scalar or vector \code{x} and return a numeric vector of the
same length. Empty \code{x} returns \code{numeric(0)}. Coefficients must be non-empty.
Non-finite numeric values follow ordinary R arithmetic.
}
\examples{
coefs <- c(5, -3, 2)
horner(c(0, 1, 2), coefs)
naivepoly(c(0, 1, 2), coefs)

}
\seealso{
Other algebra:
\code{\link[=bilinear]{bilinear()}},
\code{\link[=cubicspline]{cubicspline()}},
\code{\link{division}},
\code{\link[=fibonacci]{fibonacci()}},
\code{\link[=isPrime]{isPrime()}},
\code{\link[=linterp]{linterp()}},
\code{\link[=nthroot]{nthroot()}},
\code{\link[=polyinterp]{polyinterp()}},
\code{\link[=pwiselinterp]{pwiselinterp()}},
\code{\link[=quadratic]{quadratic()}}
}
\concept{algebra}
