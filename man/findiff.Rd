% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/findiff.R
\name{findiff}
\alias{findiff}
\alias{symdiff}
\alias{findiff2}
\alias{rdiff}
\title{Finite Differences}
\usage{
findiff(f, x, h = x * sqrt(.Machine$double.eps))

symdiff(f, x, h = x * .Machine$double.eps^(1/3))

findiff2(f, x, h)

rdiff(f, x, n = 10, h = 1e-04)
}
\arguments{
\item{f}{function to differentiate}

\item{x}{the point at which to evaluate the derivative (finite numeric
scalar)}

\item{h}{step size for evaluation (numeric scalar)}

\item{n}{maximum number of Richardson extrapolation steps in \code{rdiff}
(positive integer, default 10)}
}
\value{
A numeric scalar giving the estimated derivative.
}
\description{
Approximate derivatives using finite-difference formulas.
}
\details{
\code{findiff} uses the forward-difference formula
\code{(f(x + h) - f(x)) / h} to approximate \verb{f'(x)}.

\code{symdiff} uses the central (symmetric) difference
\verb{(f(x + h) - f(x - h)) / (2h)}, which has error O(h^2).

\code{findiff2} approximates the second derivative using the standard
three-point stencil \verb{(f(x + h) - 2f(x) + f(x - h)) / h^2}.

\code{rdiff} applies Richardson extrapolation to \code{symdiff}, recursively
combining estimates at halved step sizes up to depth \code{n}.
}
\examples{
findiff(sin, pi, 1e-3)
symdiff(sin, pi, 1e-3)

}
\concept{differentiation}
