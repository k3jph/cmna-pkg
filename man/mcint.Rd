% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcintegrate.R
\name{mcint}
\alias{mcint}
\alias{mcint2}
\title{Monte Carlo Integration}
\usage{
mcint(f, a, b, m = 1000)

mcint2(f, xdom, ydom, m = 1000)
}
\arguments{
\item{f}{function to integrate}

\item{a}{lower bound of integration (finite numeric scalar)}

\item{b}{upper bound of integration (finite numeric scalar)}

\item{m}{number of random sample points (positive integer, default 1000)}

\item{xdom}{the domain on \code{x} of integration in two dimensions}

\item{ydom}{the domain on \code{y} of integration in two dimensions}
}
\value{
A numeric scalar giving the estimated integral (or volume).
}
\description{
Estimate definite integrals by Monte Carlo sampling.
}
\details{
\code{mcint} draws \code{m} points uniformly on \verb{[a, b]}, evaluates \code{f} at
each, and returns \code{(b - a)} times the sample mean. \code{mcint2} extends
this to two dimensions: \code{m} points are drawn uniformly over the
rectangular domain defined by \code{xdom} and \code{ydom}, and the volume
estimate is the domain area times the sample mean of \code{f(x, y)}.
}
\examples{
f <- function(x) { sin(x)^2 + log(x)}
mcint(f, 0, 1)
mcint(f, 0, 1, m = 10e6)

}
\seealso{
Other integration:
\code{\link[=adaptint]{adaptint()}},
\code{\link[=gaussint]{gaussint()}},
\code{\link[=giniquintile]{giniquintile()}},
\code{\link[=midpt]{midpt()}},
\code{\link{revolution-solid}},
\code{\link[=romberg]{romberg()}},
\code{\link[=simp]{simp()}},
\code{\link[=simp38]{simp38()}},
\code{\link[=trap]{trap()}}
}
\concept{integration}
