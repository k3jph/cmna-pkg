% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/naivesum.R
\name{summation}
\alias{summation}
\alias{naivesum}
\alias{kahansum}
\alias{pwisesum}
\title{Summation algorithms}
\usage{
naivesum(x)

kahansum(x)

pwisesum(x)
}
\arguments{
\item{x}{A numeric vector. Empty vectors and \code{NULL} return zero.}
}
\value{
A numeric scalar sum.
}
\description{
Sum a numeric vector using naive, compensated, or pairwise summation.
}
\details{
\code{naivesum()} accumulates values from left to right. This is the most direct
summation algorithm, but it can lose low-order precision when many small
corrections are added to a large partial sum.

\code{kahansum()} implements Kahan compensated summation. It keeps a compensation
term for low-order information lost to floating-point rounding and reinserts
that compensation during later additions.

\code{pwisesum()} implements recursive pairwise summation. It divides the vector
into two parts, sums each part recursively, and adds the two partial sums.

All three functions require numeric input. Non-finite values are allowed and
follow base-R arithmetic: \code{NA}, \code{NaN}, \code{Inf}, and \code{-Inf} propagate according
to ordinary R addition.
}
\examples{
x <- c(1, 1e-16, 1e-16, -1)
naivesum(x)
kahansum(x)
pwisesum(x)

}
\concept{intro}
